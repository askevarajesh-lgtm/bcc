import React from 'react';
import { FileText } from 'lucide-react';
import SeoComingSoon from '../components/SeoComingSoon';

const ReportsTab = () => (
  <SeoComingSoon
    title="Reports"
    description="Scheduled and on-demand SEO performance reporting."
    icon={FileText}
  />
);

export default ReportsTab;
