import React from 'react';
import { Card, Typography } from 'antd';

const { Text } = Typography;

const MasterItemDetailsCard = ({ project, masterItem }) => {
  return (
    <Card title="Master Item Details" size="small">
      <Text type="secondary">Master Item Placeholder Component.</Text>
      {/* Implement full MasterItem details here later */}
    </Card>
  );
};

export default MasterItemDetailsCard;
